# 中断方式

## 概念
设备准备好后会主动发送**中断信号**，CPU 暂停当前程序执行中断处理。

## 工作流程
```
CPU 发起 I/O 请求
  ↓
CPU 继续执行其他程序（不再等待）
  ↓
设备完成 → 发送中断信号
  ↓
CPU 暂停当前程序，执行中断处理程序
  ↓
恢复被中断的程序
```

## 优点
- 实现了 **CPU 与设备并行工作**
- 提高了 CPU 利用率

## 缺点
- 每次传输一个字/字节都产生一次中断
- 大量数据传输时中断频繁，开销大
