# 安全状态

## 定义
如果系统能按某种顺序为每个进程分配资源（不超过其最大需求），则该状态是**安全状态**。

## 安全序列
找到一个进程序列 `<P1, P2, ..., Pn>` 使得：
- Pi 的 Need ≤ 当前可用资源 + 前面进程释放的资源

## 安全状态 vs 死锁
- **安全状态** → 一定无死锁
- **不安全状态** → 可能发生死锁（但不一定）
- **死锁** → 一定在不安全状态

## 关系图
```
安全状态 ⊂ 无死锁状态 ⊂ 所有状态
不安全状态 ⊃ 死锁状态
```
