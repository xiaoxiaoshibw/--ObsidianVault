# 溢出判断

## 根本原因
**寄存器的位数有限**（不能表示超过范围的数据）

## 三种判断方法

### 1. 单符号位 + 进位
$溢出 = C_0 \oplus C_1$
- $：符号位进位
- $：最高数值位进位
- 异或结果为1则溢出

### 2. 双符号位（模4补码）
-  = 00$：正数，无溢出
-  = 01$：**正溢出**
-  = 10$：**负溢出**
-  = 11$：负数，无溢出
- 存储时只需**一个符号位**

### 3. 硬件实现
用**异或门**实现溢出判断电路

## 左移溢出
符号位 ≠ 最高数位时，左移会发生溢出
