# 主机与外设

## 主机组成
- 运算器 + 控制器 + 主存

## 运算器不包含的部件
**地址寄存器**（在CPU内但不在运算器中）
- 运算器包含：状态寄存器、数据总线、ALU
- 地址寄存器用于保存CPU当前访问的内存单元地址

## CPU能直接访问的存储器
- **内存**、**Cache**、**寄存器**
- CPU**不能直接访问**：**硬盘**（需先调入内存）

## 计算机操作的最小单位
**时钟周期**（CPU频率的倒数）
- 指令周期 > 机器周期 > 时钟周期
- 机器周期由**存取周期**确定
